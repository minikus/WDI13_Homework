<h1>All Books</h1>
<% @books.each do |book| %>
<% if book.image.present? %>
  <%= image_tag(book.image, :class => "thumb") %>
<%end%>
  <ul>
    <li><%=link_to(book.title, book)%></li>
    <li><%= book.genre %></li>
    <li><%= book.year %></li>
  </ul>
<%end%>
